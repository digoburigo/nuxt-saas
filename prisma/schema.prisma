//////////////////////////////////////////////////////////////////////////////////////////////
// DO NOT MODIFY THIS FILE                                                                  //
// This file is automatically generated by ZenStack CLI and should not be manually updated. //
//////////////////////////////////////////////////////////////////////////////////////////////

datasource db {
    provider = "sqlite"
    url = "file:/Users/rodrigoburigo/Documents/Projects/JavaScript/nuxt-app/prisma/dev.db"
}

generator client {
    provider = "prisma-client-js"
}

/// @@allow('create', true)
/// @@allow('read', true)
/// @@allow('all', auth() == this)
model User {
    id String @id() @default(cuid())
    firstName String?
    lastName String?
    name String?
    /// @email
    email String @unique()
    /// @password
    /// @omit
    /// @length(8, 16)
    password String
    posts Post[]
    sessions Session[]
}

/// @@allow('read', auth() != null && published)
/// @@allow('all', author == auth())
model Post {
    id String @id() @default(cuid())
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt()
    /// @length(1, 256)
    title String
    content String
    published Boolean @default(false)
    author User @relation(fields: [authorId], references: [id])
    authorId String
}

model Todo {
    id String @id() @default(cuid())
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt()
    title String
    completed Boolean @default(false)
}

model Session {
    id String @id() @default(cuid())
    expiresAt DateTime
    user User @relation(references: [id], fields: [userId], onDelete: Cascade)
    userId String
}